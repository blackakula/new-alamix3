- $showMenu = $this->get('show_menu_and_logo')
- if (!isset($showMenu) || $showMenu)
  <?php
    $this->setMenu();
    $menu = $this->get('menu');
  ?>
  .menu2
    - foreach ($menu as $i => $item)
      <?php
        if (isset($item['name'])) {
          $i = '-' . $item['name'];
        }
        $class = 'item' . $i;
      ?>
      .item.$class
        %a{href="$item[1]"}
          %span= $item[0]
    .clear